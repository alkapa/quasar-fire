version: "2"
services:
  serve:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      GRPC_PORT: "50052"
      REST_PORT: "9090"
    command: sh -c "./cmd/app serve quasar"
    restart: on-failure
    ports:
      - "9090:9090"